{
  "name": "natkhat-ai",
  "version": "0.0.0",
  "private": true,
  "description": "Natkhat AI monorepo root — see PROJECT.md and docs/README.md.",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4",
  "prettier": "@natkhat-ai/config-prettier",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "check-env": "tsx scripts/check-env.ts",
    "test:check-env": "tsx scripts/check-env.spec.ts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@natkhat-ai/config-prettier": "workspace:*",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.4.0",
    "tsx": "^4.19.0",
    "turbo": "^2.3.0"
  }
}
