# Natkhat AI — root environment template
#
# No environment variables are required at the repository root as of
# Sprint 01, Milestone 1: no apps, database, auth, or storage exist
# yet (see docs/sprints/sprint-01.md, §14).
#
# Each app will ship its own .env.example once scaffolded (Milestone
# 7/8), and database/auth variables are added only after ADR-0004 /
# ADR-0005 implementation begins. Never commit a real .env file.
