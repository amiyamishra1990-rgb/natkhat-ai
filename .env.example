# Natkhat AI — root environment template
#
# No environment variables are required at the repository root itself.
# As of Sprint 03, Milestone 13 (docs/sprints/sprint-03.md, §4),
# `apps/backend` ships its own `.env.example` for local-Postgres
# development (Decision J.5) — see apps/backend/.env.example.
# Never commit a real .env file.
